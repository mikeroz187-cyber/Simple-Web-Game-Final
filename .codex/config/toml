# Codex configuration for the Studio Empire project
# Copy this file into a `.codex` directory at the project root.  When Codex is run with
# `CODEX_HOME` set to the project directory (or with `--config .codex/config.toml`),
# these settings will override global defaults.

model = "gpt-5.2-codex-xhigh"
# Require explicit approval for anything non-trivial to avoid scope creep.
approval_policy = "always"
sandbox_mode = "workspace-write"
model_reasoning_effort = "high"
# Keep MVP guardrails close; read docs/README.md and docs/15-mvp-scope.md first.
project_doc_max_bytes = 65536

[features]
shell_snapshot = true
web_search_request = true
