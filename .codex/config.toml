# Codex configuration for Studio Empire
# These settings control Codex behavior, not game balance values.

model = "gpt-5.2-codex-xhigh"
approval_policy = "always"
sandbox_mode = "workspace-write"
model_reasoning_effort = "high"
project_doc_max_bytes = 65536

[features]
shell_snapshot = true
web_search_request = true

# IMPORTANT: Always read these docs before implementing:
# 1. docs/mvp/CURRENT-SCOPE.md
# 2. docs/mvp/BUILD-ORDER.md
# 3. docs/mvp/TECHNICAL-GUARDRAILS.md
#
# The /docs/vision/ folder contains future design docs.
# Do NOT implement features from vision docs unless they appear in mvp docs.
